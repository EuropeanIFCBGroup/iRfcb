% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_roifile_to_pngs.R,
%   R/extract_images_from_ROI.R
\name{convert_roifile_to_pngs}
\alias{convert_roifile_to_pngs}
\title{Convert ROI File to PNG Images}
\usage{
convert_roifile_to_pngs(roifile)

convert_roifile_to_pngs(roifile)
}
\arguments{
\item{roifile}{A character string specifying the path to the .roi file.}
}
\value{
No return value, called for side effects. Writes PNG images to a directory.

No return value, called for side effects. Writes PNG images to a directory.
}
\description{
This function reads a .roi file and its corresponding .adc file, extracts regions of interest (ROIs),
and saves each ROI as a PNG image in a specified directory.

This function reads a .roi file and its corresponding .adc file, extracts regions of interest (ROIs),
and saves each ROI as a PNG image in a specified directory.
}
\examples{
\dontrun{
# Convert ROI file to PNG images
convert_roifile_to_pngs("your_roi_file.roi")
}
\dontrun{
# Convert ROI file to PNG images
convert_roifile_to_pngs("your_roi_file.roi")
}
}
