% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifcb_qc_sample.R
\name{ifcb_qc_sample}
\alias{ifcb_qc_sample}
\title{Quality-control a raw IFCB sample (hdr/adc/roi triplet)}
\usage{
ifcb_qc_sample(
  sample,
  data_folder = NULL,
  max_ml = NULL,
  volume_tolerance = 0.05,
  runtime_tolerance = 0.02,
  max_roi_mb = NULL,
  max_humidity = NULL,
  max_temperature = NULL,
  flowrate = 0.25
)
}
\arguments{
\item{sample}{Sample(s) to check. Either a single directory (all \code{.adc}
files within are discovered recursively), or a character vector of sample
base names or paths (with or without a \code{.hdr}/\code{.adc}/\code{.roi} extension).
When \code{data_folder} is supplied, bare sample names are resolved against it.}

\item{data_folder}{Optional directory in which to locate the triplet files
when \code{sample} is given as bare sample names. Searched recursively.}

\item{max_ml}{Optional fixed upper bound (in millilitres) for a plausible
analyzed volume, applied to every sample. Default \code{NULL} derives the ceiling
per sample from the header syringe volume (\code{SyringeSampleVolume}, falling
back to \code{syringeSize}, then the 5 mL IFCB standard) scaled by
\code{volume_tolerance}. Set this only to override that instrument-reported
value.}

\item{volume_tolerance}{Fractional tolerance added to the derived syringe
volume ceiling (default \code{0.05}, i.e. 5\%) to absorb estimation noise in the
analyzed volume. Ignored when \code{max_ml} is supplied.}

\item{runtime_tolerance}{Fractional slack by which the ADC's last-trigger run
time may exceed the header's total run time before \code{runtime_consistent} is
set to \code{FALSE} (default \code{0.02}, i.e. 2\%).}

\item{max_roi_mb}{Optional numeric upper bound (in megabytes, where
1 MB = 1024^2 bytes) for the \code{.roi} file size. When supplied, samples whose
\code{.roi} exceeds this size are flagged in the advisory \code{roi_oversized} column
(e.g. \code{max_roi_mb = 5} for 5 MB). Default \code{NULL} disables the check
(\code{roi_oversized} is \code{NA}).}

\item{max_humidity}{Optional numeric threshold (percent) for the header's
recorded \code{humidity}. Samples above it are flagged in the advisory
\code{humidity_high} column. Default \code{NULL} disables the check.}

\item{max_temperature}{Optional numeric threshold (degrees, as recorded by
the instrument) for the header's \code{temperature}. Samples above it are
flagged in the advisory \code{temperature_high} column. Default \code{NULL} disables
the check.}

\item{flowrate}{Syringe flow rate (millilitres per minute) passed to
\code{\link[=ifcb_volume_analyzed]{ifcb_volume_analyzed()}}. Default \code{0.25}.}
}
\value{
A tibble with one row per sample containing the resolved file paths,
QC metrics, boolean QC flags, and an overall \code{qc_pass} column.
}
\description{
Validates the integrity and self-consistency of one or more raw IFCB samples
and returns a tidy tibble of QC metrics and flags, one row per sample. Each
sample is expected to consist of the standard IFCB file triplet sharing a
base name: a header (\code{.hdr}), an ADC table (\code{.adc}), and the raw image data
(\code{.roi}).
}
\details{
The checks build directly on the package's existing readers
(\code{\link[=ifcb_read_hdr_data]{ifcb_read_hdr_data()}}, \code{read_adc_columns()}, \code{\link[=ifcb_volume_analyzed]{ifcb_volume_analyzed()}})
cover four areas:

\describe{
\item{Triplet completeness}{Whether all of \code{.hdr}, \code{.adc}, and \code{.roi} are
present (\code{files_complete}).}
\item{ROI count consistency}{The number of imaged ROIs in the ADC (rows
with a non-zero ROI width) must equal the header's \code{roiCount}
(\code{roi_count_match}). Note that the ADC row count is \strong{not} compared to
\code{triggerCount}: depending on the ADC format a single trigger may yield
several ROIs, so \code{triggerCount} is reported but not used as a hard
check.}
\item{ROI data completeness}{The \code{.roi} file must be at least as large as
the last image's end offset (\code{max(StartByte + width * height)}) computed
from the ADC. A smaller file indicates a truncated or aborted transfer
(\code{roi_data_complete}).}
\item{Run time consistency}{The run time recorded at the ADC's last trigger
must not exceed the header's total run time (within \code{runtime_tolerance}),
since a trigger cannot fire after acquisition has stopped
(\code{runtime_consistent}). A header run time materially shorter than the
ADC's last trigger points to corrupted or truncated header metadata; a
run that legitimately continued past the last trigger (header run time
longer than the ADC's) is normal for sparse samples and is not flagged.}
\item{Flow / volume sanity}{The analyzed volume from
\code{\link[=ifcb_volume_analyzed]{ifcb_volume_analyzed()}} must be positive and not exceed the syringe
sample volume (\code{volume_ok}). The ceiling is taken from the header's
\code{SyringeSampleVolume} (reported as \code{syringe_ml}, e.g. 5 mL for a standard
IFCB), plus \code{volume_tolerance}, since the analyzed volume can never
physically exceed the drawn syringe volume. A fixed ceiling can be forced
with \code{max_ml}.}
}

Further advisory flags are reported but do \strong{not} affect \code{qc_pass}, as they
describe valid samples that a user may nonetheless wish to exclude:
\code{is_bead_run} (a bead/calibration run, detected from the header's \code{runBeads}
field or a \code{sampleType} containing "bead"), \code{is_empty} (no imaged ROIs),
\code{roi_oversized} (the \code{.roi} file exceeds \code{max_roi_mb}, useful for catching
overloaded or anomalous runs), and \code{humidity_high} / \code{temperature_high} (the
header's recorded \code{humidity} / \code{temperature} exceed \code{max_humidity} /
\code{max_temperature}, flagging possible condensation or overheating). The latter
three are only evaluated when their threshold is supplied; otherwise they are
\code{NA}. The measured \code{humidity} and \code{temperature} are always reported.

\code{qc_pass} is the conjunction of the integrity checks above
(\code{files_complete}, \code{roi_count_match}, \code{roi_data_complete},
\code{runtime_consistent}, \code{volume_ok}). A check that cannot be evaluated for a
given sample is reported as \code{NA} and treated as \emph{not applicable}: it does not
fail \code{qc_pass}. This matters for legacy IFCB headers, which omit the
post-run \code{roiCount} summary field (so \code{roi_count_match} is \code{NA}); such samples
can still pass on the checks that do apply. Only a check that actually
evaluates to \code{FALSE} fails the sample. \code{files_complete} and \code{volume_ok} are
always \code{TRUE}/\code{FALSE} (never \code{NA}) and so always count.
}
\examples{
\dontrun{
# Check every sample in a data directory
qc <- ifcb_qc_sample("data/raw")

# Keep only clean, non-bead samples for analysis
dplyr::filter(qc, qc_pass, !is_bead_run)
}
}
\references{
Sosik, H. M. and Olson, R. J. (2007), Automated taxonomic
classification of phytoplankton sampled with imaging-in-flow cytometry.
Limnol. Oceanogr: Methods 5, 204-216.
}
\seealso{
\code{\link[=ifcb_read_hdr_data]{ifcb_read_hdr_data()}} \code{\link[=ifcb_volume_analyzed]{ifcb_volume_analyzed()}}
}
