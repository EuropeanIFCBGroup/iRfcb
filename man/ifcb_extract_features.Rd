% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifcb_extract_features.R
\name{ifcb_extract_features}
\alias{ifcb_extract_features}
\title{Extract Slim Features and Blobs from IFCB Data}
\usage{
ifcb_extract_features(
  data_folder,
  features_folder,
  blobs_folder,
  bins = NULL,
  parallel = FALSE,
  n_cores = NULL,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{data_folder}{The path to a directory containing raw IFCB data
(\code{.roi}, \code{.adc} and \code{.hdr} files). The directory is searched recursively by
\code{pyifcb}, so nested data structures are supported.}

\item{features_folder}{The path to the directory where the
\verb{<bin>_features_v4.csv} files will be written. Created if it does not exist.}

\item{blobs_folder}{The path to the directory where the \verb{<bin>_blobs_v4.zip}
files will be written. Created if it does not exist.}

\item{bins}{An optional character vector of bin names (e.g.
\code{"D20220522T003051_IFCB134"}) to restrict processing to a subset of bins.
If \code{NULL} (default), all bins found in \code{data_folder} are processed.}

\item{parallel}{A logical indicating whether to process bins in parallel.
Default is \code{FALSE}.}

\item{n_cores}{An integer specifying the number of worker processes to use
when \code{parallel = TRUE}. If \code{NULL} (default), \code{parallel::detectCores() - 1}
cores are used. Ignored when \code{parallel = FALSE}.}

\item{overwrite}{A logical indicating whether to overwrite existing feature
and blob files. If \code{FALSE} (default), bins whose outputs already exist are
skipped.}

\item{verbose}{A logical indicating whether to print progress messages,
including a progress bar that advances as each bin is processed.
Default is \code{TRUE}.}
}
\value{
Invisibly returns a tibble with one row per bin and the columns
\code{bin}, \code{status} (\code{"processed"}, \code{"skipped"} or \code{"error"}) and \code{message}.
The function is primarily called for its side effect of writing feature and
blob files to disk.
}
\description{
This function computes the "slim" feature set (version 4) and blob masks from
raw Imaging FlowCytobot (IFCB) data by calling the WHOI \code{ifcb-features} Python
package. For each bin it writes a feature table
(\verb{<bin>_features_v4.csv}, 30 morphological features per region of interest)
and an archive of binary blob masks (\verb{<bin>_blobs_v4.zip}, one 1-bit PNG per
ROI). Features and blobs are written to separate, user-specified directories.
}
\details{
This function wraps the \code{extract_slim_features} workflow from the
\code{ifcb-features} Python repository, which can be found at
\url{https://github.com/WHOIGit/ifcb-features}.

Python and the \code{ifcb-features} package must be installed to use this function.
The required Python packages can be installed in a virtual environment using
\code{ifcb_py_install(features = TRUE)}, which additionally installs \code{ifcb-features}
and its dependencies (\code{pyifcb}, \code{phasepack}, \code{scikit-image}, \code{scikit-learn}).

\strong{Python version requirement:} \code{pyifcb} and its dependencies (notably
\code{h5py}) must be available as binary wheels for your Python version;
installation will fail if source compilation is required and the build
environment is incompatible. See
\url{https://github.com/WHOIGit/ifcb-features} for current Python version
requirements, and use \code{ifcb_py_install(features = TRUE)} to install into a
compatible environment.

Bins are processed sequentially by default. When \code{parallel = TRUE}, bins are
distributed across \code{n_cores} worker processes on the Python side, which can
substantially reduce runtime for large datasets. Existing outputs are skipped
unless \code{overwrite = TRUE}, so the function can be re-run to resume an
interrupted extraction.
}
\examples{
\dontrun{
# Install the Python environment including ifcb-features
ifcb_py_install(features = TRUE)

# Extract features and blobs from all bins in a data folder
ifcb_extract_features(
  data_folder = "path/to/data",
  features_folder = "path/to/features",
  blobs_folder = "path/to/blobs"
)

# Process a subset of bins in parallel using 4 cores
ifcb_extract_features(
  data_folder = "path/to/data",
  features_folder = "path/to/features",
  blobs_folder = "path/to/blobs",
  bins = c("D20220522T003051_IFCB134", "D20220522T000439_IFCB134"),
  parallel = TRUE,
  n_cores = 4
)
}

}
\seealso{
\code{\link{ifcb_py_install}}, \code{\link{ifcb_read_features}},
\url{https://github.com/WHOIGit/ifcb-features}
}
